# 🖥️ Подключение к серверу 77.73.232.84

## 🎯 Лучший способ: Remote SSH в Cursor

В Cursor уже есть настройка для этого сервера! Используем Remote SSH для прямого управления.

---

## 📋 Способ 1: Remote SSH в Cursor (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Настройка SSH Config

1. Создай/открой файл SSH config:
   - Путь: `C:\Users\User\.ssh\config`
   
2. Добавь эту конфигурацию:

```
Host mcp-server
    HostName 77.73.232.84
    User root
    Port 22
```

### Шаг 2: Подключение через Cursor

1. Нажми `F1` или `Ctrl+Shift+P`
2. Введи: **"Remote-SSH: Connect to Host"**
3. Выбери: **mcp-server** (или введи `root@77.73.232.84`)
4. Введи пароль когда попросит
5. Готово! Cursor откроется на удалённом сервере

### Преимущества:
- ✅ Прямое редактирование файлов на сервере
- ✅ Терминал работает на сервере
- ✅ Можно запускать команды напрямую
- ✅ Работа как с локальными файлами

---

## 📋 Способ 2: PowerShell/Terminal

### Простое подключение:

```powershell
ssh root@77.73.232.84
```

Введи пароль когда попросит.

### После подключения - проверка статуса:

```bash
# Информация о системе
uname -a

# Версия ОС
cat /etc/os-release

# Проверка процессов MCP
ps aux | grep mcp

# Версия Python
python3 --version

# Свободное место
df -h

# Использование памяти
free -h

# Какие порты открыты
netstat -tlnp | grep LISTEN
```

---

## 📋 Способ 3: Скрипт для автоматической проверки

Создам скрипт, который соберёт всю информацию о сервере одной командой.

### На сервере создай файл:

```bash
cat > check_server.sh << 'EOF'
#!/bin/bash
echo "=================================="
echo "MCP Server Status Check"
echo "=================================="
echo ""
echo "=== System Info ==="
uname -a
echo ""
echo "=== OS Version ==="
cat /etc/os-release | head -5
echo ""
echo "=== Python ==="
python3 --version 2>/dev/null || python --version
echo ""
echo "=== MCP Processes ==="
ps aux | grep -E 'mcp|python.*server' | grep -v grep
echo ""
echo "=== Listening Ports ==="
netstat -tlnp 2>/dev/null | grep LISTEN || ss -tlnp | grep LISTEN
echo ""
echo "=== Disk Space ==="
df -h | grep -E '^Filesystem|^/'
echo ""
echo "=== Memory ==="
free -h
echo ""
echo "=== Uptime ==="
uptime
echo ""
echo "=== Network Interfaces ==="
ip addr show | grep -E 'inet |^[0-9]'
echo ""
echo "=================================="
EOF

chmod +x check_server.sh
./check_server.sh
```

---

## 📋 Способ 4: SSH ключ (для постоянной работы без пароля)

### Если хочешь подключаться без пароля каждый раз:

1. **На локальной машине** (Windows):
   ```powershell
   ssh-keygen -t rsa -b 4096
   ```
   Нажимай Enter (можно без passphrase)

2. **Скопируй ключ на сервер**:
   ```powershell
   type C:\Users\User\.ssh\id_rsa.pub | ssh root@77.73.232.84 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
   ```
   Введи пароль последний раз

3. **Теперь подключайся без пароля**:
   ```powershell
   ssh root@77.73.232.84
   ```

---

## 🚀 Быстрая диагностика сервера

После подключения к серверу, запусти эти команды:

```bash
# 1. Проверка MCP сервера
cd /root  # или где установлен проект
ls -la | grep mcp

# 2. Проверка Python окружения
which python3
python3 -m pip list | grep -E 'mcp|sse|fastapi'

# 3. Проверка запущенных сервисов
systemctl status mcp* 2>/dev/null
ps aux | grep python

# 4. Проверка логов (если есть)
tail -50 /var/log/syslog | grep -i mcp
tail -50 cloudflared.log 2>/dev/null
```

---

## 📝 Частые команды для управления сервером

```bash
# Перезапуск MCP сервера (если установлен как сервис)
systemctl restart mcp-server

# Просмотр логов в реальном времени
tail -f /path/to/logs/mcp.log

# Обновление кода с GitHub
cd /path/to/project
git pull origin main

# Установка зависимостей
pip3 install -r requirements.txt

# Запуск сервера вручную
python3 mcp_sse_server.py
```

---

## 🔧 Создание SSH Config файла (детально)

### Windows:

1. Открой PowerShell:
   ```powershell
   mkdir C:\Users\User\.ssh -Force
   notepad C:\Users\User\.ssh\config
   ```

2. Вставь конфигурацию:
   ```
   Host mcp-server
       HostName 77.73.232.84
       User root
       Port 22
       ServerAliveInterval 60
       ServerAliveCountMax 3
   ```

3. Сохрани файл

---

## ✅ Проверь подключение

```powershell
# Простой тест
ssh root@77.73.232.84 "echo 'Connection OK' && uptime"
```

Если нужна помощь с конкретной командой или настройкой - пиши!
